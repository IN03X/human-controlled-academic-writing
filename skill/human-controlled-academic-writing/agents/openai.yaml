interface:
  display_name: "Human-Controlled Academic Writing"
  short_description: "Human-led paper writing, evidence checks, review, and sync."
  default_prompt: "Use $human-controlled-academic-writing to help with this paper while preserving human control over its claims and scope."

policy:
  allow_implicit_invocation: true

